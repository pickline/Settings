return {
    ensure_installed = {
        "c", "cpp", "lua", "go", "gomod", "gitignore", "gowork", "vim", "python",
        "bash", "dockerfile", "yaml", "toml", "json", "make", "cmake", "markdown"
    },
    sync_install = true,
    auto_install = true,
    highlight = {
        enable = true,
        additional_vim_regex_highlighting = false,
    }
}